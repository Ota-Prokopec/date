import { Textarea } from '@heroui/react'

type BioInputProps = {}

export const BioInput = ({}: BioInputProps) => {
  return <Textarea className=" outline-none !border-0 w-full" style={{ border: '0 !important' }} />
}
